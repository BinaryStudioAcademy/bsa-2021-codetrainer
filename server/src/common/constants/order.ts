export enum Order {
	ASC = 'ASC',
	DESC = 'DESC',
}

export enum ClansOrderByOptions {
	BY_SIZE = 'numberOfMembers',
	BY_RANK = 'rank',
	BY_TIME = 'createdAt',
	BY_NAME = 'name',
	BY_HONOR = 'honor',
}
