import React from 'react';
import styles from './language-choice.module.scss'

interface ILandingLanguagesProps {}
export const LanguageChoice = (props: ILandingLanguagesProps) => {
	return <div className={styles.languageChoice}>
        choice
	</div>;
};
